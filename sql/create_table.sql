CREATE TABLE quality_of_life_index (
    rank INTEGER,
    country VARCHAR(20),
    quality_of_life_index DECIMAL,
    purchasing_power_index DECIMAL,
    safety_index DECIMAL,
    health_care_index DECIMAL,
    cost_of_living_index DECIMAL,
    property_price_to_income_ratio DECIMAL,
    traffic_commute_time_index DECIMAL,
    pollution_index DECIMAL,
    climate_index DECIMAL,
    year_data txt
);